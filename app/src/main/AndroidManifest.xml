<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Hiazee"
        tools:targetApi="31">
        <activity
            android:name=".ui.activities.OrderPaymentDetailsActivity"
            android:exported="false"
            android:label="@string/title_activity_order_payment_details"
            android:theme="@style/Theme.Hiazee" />
        <activity
            android:name=".ui.activities.OrderDetailsActivity"
            android:exported="false"
            android:label="@string/title_activity_order_details"
            android:theme="@style/Theme.Hiazee" />
        <activity
            android:name=".ui.activities.OrderListActivity"
            android:exported="false"
            android:label="@string/title_activity_order_list"
            android:theme="@style/Theme.Hiazee" />
        <activity
            android:name=".ui.activities.AddShipAddressActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.ShipAddressActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.LoginActivity"
            android:exported="true" />
        <activity
            android:name=".ui.activities.CartActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>